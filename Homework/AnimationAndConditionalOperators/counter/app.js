//Variable for the number
let number = 0;

function setup() {

}

function draw() {

    //Adds 1 to the number variable every frame
    number += 1;

    //Prints the value of the number variable in the console
    print(number);
}